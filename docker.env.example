# Optional overrides for `docker compose`.
# Copy to `.env` in the repo root if you want SMTP or a stronger JWT.

JWT_SECRET=change-me-in-production

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM=
